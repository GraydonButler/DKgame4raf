using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mydayonescript : MonoBehaviour
{   
    // data types
    /*hi*/
    // Start is called before the first frame update
    void Start()
    {
        int intVar = 4; //100 
        string stringVar = "graydon buhtolah";
        float floatVar = 5.5f;
        bool boolVar = false;
        Debug.Log(stringVar) ;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}